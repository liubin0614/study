#### 函数范型
```js
function id<T>(arg:T):T {
  return arg;
}
```

#### 接口的泛型写法

```js
interface Box<Type> {
  contents: Type;
}

let box:Box<string>;
```

#### 类的泛型写法
```js
class Pair<K, V> {
  key: K;
  value: V;
}
// 继承
class A<T> {
  value: T;
}
class B extends A<any> {
}
```